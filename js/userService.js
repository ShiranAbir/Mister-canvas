'use strict'

function setColor(color) {
    gUserData.color = color
    console.log('New color is:', color)
}

function setShape(shape) {
    gUserData.shape = shape
    console.log('New shape is:', shape)
}